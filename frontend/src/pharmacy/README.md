Pharmacy frontend

Add routes to your router:
- /pharmacy — PharmacistDashboard
- /pharmacy/fulfill/:id — FulfillPrescription

The pages expect existing axios auth interceptor and protected routes to be available.
