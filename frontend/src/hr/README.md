HR Frontend

Components:
- `HRDashboard` — summary of today's attendance & overtime
- `EmployeeForm` — create a new employee
- `AttendanceReport` — filterable attendance report
- `PayrollReport` — payroll generation and results

NOTE: Components expect existing auth provider and axios instance from `frontend/src/auth/api.ts`.
